# 复制为 .env；npm start 和 Windows 启动器会用 Node 内置解析器加载它。
# 终端中已经设置的环境变量优先。
AI_PROVIDER=ollama
PORT=4318

# Ollama：本地推理，不需要 API key。请安装 Ollama 并下载此模型。
# 推荐模型约 3.4 GB。自动准备要求至少 12 GiB 内存；建议 16 GB 内存和独立显卡。
# CPU 推理可能耗时数分钟或超时；低配置可在网页选择在线 AI。估分不是官方成绩。
OLLAMA_HOST=http://127.0.0.1:11434
OLLAMA_MODEL=qwen3.5:4b

# 在线 AI 推荐直接点网页“设置在线 AI”；也可用以下环境变量。
# AI_BASE_URL=https://your-provider.example/v1
# OPENAI_MODEL=your-provider-model-name
# AI_API_KEY=replace-with-your-server-side-key

# 可选 Codex：使用你自己安装并登录的 CLI 和账号额度。
# AI_PROVIDER=codex
# IELTS_CODEX_PATH=codex.exe

# 未设置提供商专用模型时，AI_MODEL 仍作为旧配置后备值。
