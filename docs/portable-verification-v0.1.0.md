# v0.1.0 Windows 便携包验证

验证日期：2026 年 9 月 17 日。

- 应用来自 Git 标签 `v0.1.0`（提交 `8107fcd90b12d463dcfc7faf25594c9273c321db`），打包时保留标签中的源文件。
- 独立便携启动器和中文说明位于仓库 `scripts/portable/`；包装脚本只为当前进程选择随包 Node，不修改系统 PATH。
- 内含 Node.js `v24.19.0` 官方 Windows x64 完整发行包及许可证，下载时核对官方 SHA-256。
- 解压到含中文和空格的路径后，在 PATH 没有系统 Node 的情况下启动成功，页面返回 HTTP 200；进程路径确认使用随包的 `node.exe`。
- 使用随包 Node 运行应用的 56 项测试，全部通过，没有失败或跳过。
- 检查发布 ZIP，未包含本机 `.env`、日志、快捷方式、真实作文结果或 `.git`。测试使用独立解压副本，测试生成的文件没有进入下载包。

下载文件：`ielts-writing-studio-v0.1.0-windows-x64.zip`，40,629,209 字节。

SHA-256：

```text
7a4d5ead876a3482dcf7abb524da2265a044e25b4d7cf2ff86ed9f3f01860783
```

此验证确认本地网页可启动及既有测试通过。便携包不包含 AI 模型；实际模型质量的验证范围见[应用验证记录](release-verification-v0.1.0.md)。
