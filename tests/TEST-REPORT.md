# 验证记录

日期：2026-09-16。环境：Windows、本机 Node、Codex 已配置模型、Codex 内置浏览器。

## 自动测试

运行 `node --test tests/backend.test.mjs tests/frontend.test.mjs tests/state.test.mjs`：35 项全部通过。

- 16 项后端测试：输入边界、四项评分、来源片段及唯一性、独立范文输入隔离、同源检查、路径保护、请求体限制、并发锁、取消、错误处理及无演示回退。
- 11 项前端逻辑测试：HTML 转义、重叠句子与搭配标注、重复词定位、跨段标注、词数、答案对照、复习间隔、收藏去重、示例数据完整性。
- 8 项状态回归测试：重试取消或失败恢复、运行中保存、示例不计入真实成绩、历史时间不随查看改变、完整分析不被空记录覆盖、编辑时保留旧版本。

## 真实 AI 验证

1. 通过 HTTP 提交「公共交通是否免费」题目：原文与精修均 273 词、4 段，识别 8 处问题；独立范文 314 词。215 秒完成；全部结构、分数、唯一引用校验通过。记录见 `live-result.json`。
2. 通过网页实际填写、选择目标 Band 7.5 并提交「居家办公与办公室办公」题目：277 词原文和精修、318 词独立范文、6 处问题、8 个搭配、4 个论证讲解。三栏参考区间为 6.5–7.0、6.5–7.0、7.5–8.0，未机械提高精修后的总分。刷新后全部结果保留。

## 浏览器交互验证

- 示例有明确标记，空题目提交显示具体提醒。
- 分析中输入锁定、两个加载面板与计时显示正常。
- 原文与精修的错误位置联动；长引用只高亮实际变化的单词。
- 嵌入好句中的搭配与整句论证仍可分别点击。
- 同类练习正确答案识别、收藏、遮住答案自测、记住后延期及刷新持久化正常。
- 范文默认折叠，可展开、收起并复制；复制实测为完整 318 词。
- 学习笔记下载成功，实际读取文件核对题目、三篇作文、评分、重点与复习内容齐全。
- 重新分析后取消，旧分数和正文保留；从练习记录可以完整恢复。
- 390px 手机、768px 平板与默认桌面测试无横向溢出。移动端三栏标签与方向键切换正常；弹窗适配并锁定背景滚动。
- 浏览器无应用错误日志。服务错误日志为空。
- 停止测试服务后通过 `Start Writing Studio.cmd` 实际重新启动成功；后台运行，AI 状态正常。

## 使用边界

当前为本机 Task 2 网页。AI 生成内容和评分存在波动，以上是已执行的验证，不是正式成绩保证。真实分析依赖网络、Codex 登录及账户额度；数据保存在当前浏览器，导出可备份。
